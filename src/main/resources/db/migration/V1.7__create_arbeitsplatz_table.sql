CREATE TABLE Arbeitsplatz (
                         mitarbeiterid int NOT NULL AUTO_INCREMENT,
                         arbeitsplatzName varchar(255),
                         strasse varchar(255),
                         strasseNumber int,
                         plz varchar(255),
                         PRIMARY KEY (mitarbeiterid)
);
